name: 💡 Менеджер задач

on:
  workflow_dispatch:
    inputs:
      action:
        description: 'Действие'
        required: true
        type: choice
        options:
          - добавить
          - список
          - завершить
          - статистика
      task:
        description: 'Название задачи (для добавления)'
        required: false
        type: string
      # ... остальные параметры